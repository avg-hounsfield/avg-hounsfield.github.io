# Capstone report files
CAPSTONE_PROJECT_REPORT.*

# Development tools and build scripts
tools/
# ...except the static page generator (part of the project)
!tools/generate_static_pages.py
!tools/test_generate_static_pages.py
!tools/test_normalize.py

# ML models (large binaries kept out of git by default).
# Tracked exceptions (added via `git add -f`): models/acr-minilm/,
# models/intent-classifier/, models/student-radiology/.
models/

# Archive and scratch folders
_archive/
_ul
_ul-*
workers/

# Variant/experimental branches (ManuMele, Otto)
*-ManuMele*
*-Otto*

# Database files
*.db

# Backup files
*.bak

# Local snapshot taken by tools/reorganize_regions.py
data/regions.backup/

# Stray empty file
link

# wandb runs from local training experiments
wandb/

# Local training checkpoints (real checkpoints live in radiology-ai-assistant repo)
checkpoints/

# Data files not served to users
data/source/
data/training/
data/schema/
data/protocols-enriched*.json

# Search index build artifacts
data/search/scenario_embeddings_layperson.npy
data/search/*layperson*
data/search/tfidf-index-debug.json

# Claude local settings variants
.claude/settings.local-*.json

# Brainstorming session files (visual companion server output)
.superpowers/
